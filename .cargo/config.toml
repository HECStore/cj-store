# Adapted from bevy/.cargo/config_fast_builds — fast-build linker + share-generics
# knobs. Active config for this project; nightly-only `-Zshare-generics=y` is
# guarded by the rust-toolchain.toml pin.
#
# CAVEAT: Cargo's precedence rule means a user-set `RUSTFLAGS` env var
# REPLACES (not appends to) the `rustflags` entries below. Contributors who
# want extra flags (e.g. `RUSTFLAGS="-C target-cpu=native"`) must re-include
# `-Clink-arg=-fuse-ld=lld -Zshare-generics=y` on Linux to keep the fast-
# build path. The macOS/aarch64 stanzas behave the same way.

# Robust git-source fetching for vendored git deps (azalea-rs, simdnbt).
# Cargo's built-in libgit2 client periodically hits transient GitHub rate
# limits on CI runners; `git-fetch-with-cli = true` shells out to the
# system `git` binary, which uses the user's credentials and is more
# resilient to slow networks and rate-limit hiccups.
[net]
git-fetch-with-cli = true

[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-Clink-arg=-fuse-ld=lld", "-Zshare-generics=y"]

# NOTE: both macOS stanzas below pin a brew-installed Mach-O LLD Port path
# (https://lld.llvm.org/MachO/index.html). Install with `brew install llvm`.
# The Intel and Apple-silicon stanzas hard-code different prefixes because
# Homebrew installs to `/usr/local/opt` on Intel and `/opt/homebrew/opt` on
# Apple silicon — both required for the linker pin to resolve.
# macOS is not exercised by CI; see DEVELOPMENT.md.
[target.x86_64-apple-darwin]
rustflags = [
    "-C",
    "link-arg=-fuse-ld=/usr/local/opt/llvm/bin/ld64.lld",
    "-Zshare-generics=y",
]

[target.aarch64-apple-darwin]
rustflags = [
    "-C",
    "link-arg=-fuse-ld=/opt/homebrew/opt/llvm/bin/ld64.lld",
    "-Zshare-generics=y",
]

# No Windows MSVC overrides: tried `linker = "rust-lld.exe"` plus
# `-Zshare-generics=n`, hit linker errors on this codebase. Default MSVC
# linker is preferred per upstream Bevy guidance for share-generics.

# Optional: Uncommenting the following improves compile times, but reduces the amount of debug info to 'line number tables only'
# In most cases the gains are negligible, but if you are on macos and have slow compile times you should see significant gains.
#[profile.dev]
#debug = 1
